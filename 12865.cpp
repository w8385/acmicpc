#include<stdio.h>
int check(int n, int count){
	n >> count;
	if(n == 1) return 1;
	else return 0;
}
int main(){
	int N, K;
	int arr[2][105] = {};
	scanf("%d%d",&N,&K);
	for(int i = 0; i < N; i++)
		scanf("%d%D",&arr[0][i],&arr[1][i]);
	}
	int cur = 1;
	while(cur++){
		for(int i = 0; i < N; i++){
			
			
}
